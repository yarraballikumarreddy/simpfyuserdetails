import React from 'react';
import "./Spinner.css";

const Spinner = () => {
    return (
      <React.Fragment>
        <div className="spinner"></div>
      </React.Fragment>
    );
}

export default Spinner

